x = 1.32
y = 3
z = 6

# round() = round the number to the nearest whole number
# result = round(x)

# abs() = is the distance a way from zero as a whole number
# result = abs(y)

# pow() = calculate the power of a number
# result = pow(y, 4)

# max() = find the max value within the function
# result = max(x, y, z)

# min() = finds the minimum value within the function.
result = min(x, y, z)

print(result)